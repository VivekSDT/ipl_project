# Frontend (React)

This is a basic React app using Material UI and react-google-charts.
It expects the Django backend to be running on http://localhost:8000 and the package.json proxy is set.

Quick start:
From main directory follow the instructions
```bash
cd frontend
npm install
npm start
```

Open http://localhost:3000
